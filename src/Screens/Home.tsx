import { View } from "react-native"
import styles from "../styles"

interface Props {
    navigation: any,
    route: any,
}

function HomeScreen({navigation, route}: Props): JSX.Element {
    return <View style={styles.Screen}>

    </View>
}

export default HomeScreen